post:
  summary: Hashes a text into various formats
  operationId: hashString
  description: |
    Hashes given text into various formats
  tags:
    - Utils
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../schemas/utils/hash/payload.yaml"

  responses:
    '200':
      $ref: "../../responses/utils/hash.yaml"
    '400':
      $ref: "../../responses/error/badrequest.yaml"
    default:
      $ref: "../../responses/error/unexpected.yaml"