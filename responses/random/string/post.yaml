# 200
description: Expected Response
content:
  application/json:
    schema:
      allOf:
      - $ref: "../../../schemas/success.yaml"
      - type: object
        properties:
          data:
            $ref: "../../../schemas/random/string/post/response.yaml"