description: Expected Response
content:
  application/json:
    schema:
      allOf:
      - $ref: "../../../schemas/success.yaml"
      - type: object
        properties:
          data:
            $ref: "../../../schemas/mockup/internet/get/response.yaml"